/*
    This language is
    really starting to shape up.
    It will replace Bash.

    /* Nested comment test */
*/

print((38. + 4) * .5)
// anonymous block
block:
    x: float = 5.5 // lexical scoping
    print(x + x)
x: float = 7.7
print(x - 0.7)
