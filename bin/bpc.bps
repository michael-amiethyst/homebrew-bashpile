#!/mnt/c/Users/desig/win-dev/bashpile/bin/bpc.bps

// get script directory
SOURCE: str = #(${BASH_SOURCE[0]})
DIR: str    = $(dirname "$SOURCE")

PROJECT_PATH: str = "$DIR/.."
JAR_PATH: str     = "${PROJECT_PATH}/target/bashpile-jar-with-dependencies.jar"
#(java -jar "$JAR_PATH" "$1" | tee output.txt)
TRANSLATED_FILENAME: str = $(tail --lines 1 < output.txt)
#(shift)
print("Start of $TRANSLATED_FILENAME")
#(./"$TRANSLATED_FILENAME" "$@")
#(rm -f "$TRANSLATED_FILENAME" output.txt)