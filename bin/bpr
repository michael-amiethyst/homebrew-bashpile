#!/usr/bin/env bash

#
# Generated from bin/bpr.bps on 2024-03-06T20:35:56.843693607-08:00[America/Los_Angeles] (timestamp 1709786156843)
#
# strict mode header
set -eEuo pipefail -o posix
declare -x IFS=$'\n\t'
declare -i s
trap 's=$?; echo "Error (exit code $s) found on line $LINENO.  Command was: $BASH_COMMAND"; exit $s' ERR
# assign statement, Bashpile line 9
declare opts
opts="o:c"
# assign statement, Bashpile line 10
declare translatedFilename
translatedFilename="command.bpt"
# assign statement, Bashpile line 11
declare commandMode
commandMode=false
# while statement, Bashpile line 13
while getopts ${opts} option; do
    # switch statement, Bashpile line 14
    case ${option} in
        "o")
            # reassign statement, Bashpile line 16
            translatedFilename=${OPTARG}
            ;;
        "c")
            # reassign statement, Bashpile line 18
            commandMode=true
            ;;
    esac
done
shift $((OPTIND - 1))
# assign statement, Bashpile line 22
declare jarPath
jarPath=$(dirname "${BASH_SOURCE[0]}")/bashpile.jar
if ! ${commandMode} && [ -z "${1+default}" ]; then
    # expression statement, Bashpile line 26
    trap - ERR
    # expression statement, Bashpile line 27
    java -jar "$jarPath"
    # expression statement, Bashpile line 28
    exit $?
fi
if ! ${commandMode}; then
    if [ "${translatedFilename}" == "command.bpt" ]; then
        # reassign statement, Bashpile line 34
        translatedFilename=$1.bpt
    fi
    # creates statement, Bashpile line 35
    if (set -o noclobber; java -jar "$jarPath" --outputFile="$translatedFilename" "$1") 2> /dev/null; then
        trap 'rm -f "$translatedFilename"; exit 10' INT TERM EXIT
        ## wrapped body of creates statement
        # expression statement, Bashpile line 37
        sleep .2
        # expression statement, Bashpile line 38
        shift
        # print statement, Bashpile line 39
        printf "Start of ${translatedFilename}\n"
        # expression statement, Bashpile line 41
        chmod 755 $translatedFilename
        # creates statement, Bashpile line 43
        if (set -o noclobber; ./"$translatedFilename" "$@" 2>&1 | tee "bpr.log") 2> /dev/null; then
            trap 'rm -f "$translatedFilename" bpr.log; exit 10' INT TERM EXIT
            ## wrapped body of creates statement
            if cat bpr.log | grep -E 'unexpected EOF|syntax error|command not found' > /dev/null 2>&1; then
                # expression statement, Bashpile line 46
                ./"$translatedFilename" "$@"
            fi
            ## end of wrapped body of creates statement
            rm -f bpr.log
            trap - INT TERM EXIT
        else
            printf "Failed to create pr.log correctly."
            rm -f bpr.log
            exit 1
        fi
        declare -i __bp_exitCode=$?
        if [ "$__bp_exitCode" -ne 0 ]; then exit "$__bp_exitCode"; fi
        ## end of wrapped body of creates statement
        rm -f "$translatedFilename"
        trap - INT TERM EXIT
    else
        printf "Failed to create translatedFilename correctly."
        rm -f "$translatedFilename"
        exit 1
    fi
    declare -i __bp_exitCode=$?
    if [ "$__bp_exitCode" -ne 0 ]; then exit "$__bp_exitCode"; fi
else
    # assign statement, Bashpile line 49
    declare command
    if [ -n "${1+default}" ]; then
        # reassign statement, Bashpile line 51
        command=$1
        # expression statement, Bashpile line 53
        shift
    else
        # reassign statement, Bashpile line 55
        command=$(cat -)
    fi
    # creates statement, Bashpile line 57
    if (set -o noclobber; java -jar "$jarPath" --outputFile="$translatedFilename" -c "$command" 1>/dev/null) 2> /dev/null; then
        trap 'rm -f "$translatedFilename"; exit 10' INT TERM EXIT
        ## wrapped body of creates statement
        # expression statement, Bashpile line 59
        sleep .2
        # expression statement, Bashpile line 60
        chmod 755 $translatedFilename
        # creates statement, Bashpile line 62
        if (set -o noclobber; ./"$translatedFilename" "$@" 2>&1 | tee "bpr.log") 2> /dev/null; then
            trap 'rm -f "$translatedFilename" bpr.log; exit 10' INT TERM EXIT
            ## wrapped body of creates statement
            if cat bpr.log | grep -E 'unexpected EOF|syntax error|command not found' > /dev/null 2>&1; then
                # expression statement, Bashpile line 64
                ./"$translatedFilename" "$@"
            fi
            ## end of wrapped body of creates statement
            rm -f bpr.log
            trap - INT TERM EXIT
        else
            printf "Failed to create pr.log correctly."
            rm -f bpr.log
            exit 1
        fi
        declare -i __bp_exitCode=$?
        if [ "$__bp_exitCode" -ne 0 ]; then exit "$__bp_exitCode"; fi
        ## end of wrapped body of creates statement
        rm -f "$translatedFilename"
        trap - INT TERM EXIT
    else
        printf "Failed to create translatedFilename correctly."
        rm -f "$translatedFilename"
        exit 1
    fi
    declare -i __bp_exitCode=$?
    if [ "$__bp_exitCode" -ne 0 ]; then exit "$__bp_exitCode"; fi
fi
