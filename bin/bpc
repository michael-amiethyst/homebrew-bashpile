#!/mnt/c/Users/desig/win-dev/bashpile/bin/bpr

// get script directory
source: str = #(${BASH_SOURCE[0]})
scriptDir: str    = $(dirname "$source")

projectDir: str = "$scriptDir/.."
jarPath: str     = "${projectDir}/target/bashpile-jar-with-dependencies.jar"
#(java -jar "$jarPath" "$@")