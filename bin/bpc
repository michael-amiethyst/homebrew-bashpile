#!/mnt/c/Users/desig/win-dev/bashpile/bin/bpr

// get script directory
scriptDir: str = $(dirname "${BASH_SOURCE[0]}")

jarPath  : str = scriptDir + "/../target/bashpile-jar-with-dependencies.jar"
#(java -jar "$jarPath" "$@")