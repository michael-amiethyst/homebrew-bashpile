#!/usr/bin/env bash

export PATH="$PATH:$HOME/.jenv/shims:/usr/local/bin:/usr/bin:/bin"
if which jenv >/dev/null; then
  eval "$(jenv init -)"
fi
target/bpc --outputFile target/bpr bin/bpr.bps
printf "Created bpr in directory %s/target" "$PWD"